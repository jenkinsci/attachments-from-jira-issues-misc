--- C:/Users/jfigler/AppData/Local/Temp/FPRPublisher.java-revBASE.svn002.tmp.java	Wed Nov 28 05:19:02 2012
+++ C:/source/Jenkins/plugins/fortify360/src/main/java/org/jvnet/hudson/plugins/fortify360/FPRPublisher.java	Wed Oct 23 07:33:06 2013
@@ -136,6 +136,8 @@ public class FPRPublisher extends Recorder {
 	@Override
 	public boolean perform(AbstractBuild<?, ?> build, Launcher launcher, BuildListener listener) throws InterruptedException, IOException {
 		
+		boolean retval = true;
+		
 		PrintStream log = launcher.getListener().getLogger();		
 		log.println("Publishing Fortify 360 FPR Data");
 		
@@ -180,6 +182,8 @@ public class FPRPublisher extends Recorder {
 			} catch ( Throwable t ) {
 				log.println("Error uploading to F360 Server: " + DESCRIPTOR.getUrl());
 				t.printStackTrace(log);
+				log.println("This build is being marked FAILED because the results were not successfully uploaded to the F360 server.");
+				retval = false;
 			} finally {
 				// if this is a remote FPR, I need to delete the local temp FPR after use
 				if ( summary.getFprFile().isRemote() ) {
@@ -229,7 +233,7 @@ public class FPRPublisher extends Recorder {
 			}
 		}
 		
-		return true;
+		return retval;
 	}
 		
 	private void jobAssignment(AbstractBuild<?, ?> build, PrintStream log) throws Exception {
