Index: maven-plugin/src/main/java/hudson/maven/MavenBuild.java
===================================================================
--- maven-plugin/src/main/java/hudson/maven/MavenBuild.java	(revision 16920)
+++ maven-plugin/src/main/java/hudson/maven/MavenBuild.java	(working copy)
@@ -174,6 +174,13 @@
         projectActionReporters.add(reporter);
     }
 
+    public List<MavenReporter> getProjectActionReporters() {
+        if(projectActionReporters == null)
+            return Collections.emptyList();
+        else
+            return Collections.unmodifiableList(projectActionReporters);
+    }
+
     public List<ExecutedMojo> getExecutedMojos() {
         if(executedMojos==null)
             return Collections.emptyList();
Index: maven-plugin/src/main/java/hudson/maven/reporters/SurefireArchiver.java
===================================================================
--- maven-plugin/src/main/java/hudson/maven/reporters/SurefireArchiver.java	(revision 16920)
+++ maven-plugin/src/main/java/hudson/maven/reporters/SurefireArchiver.java	(working copy)
@@ -23,6 +23,7 @@
  */
 package hudson.maven.reporters;
 
+import hudson.Launcher;
 import hudson.Util;
 import hudson.Extension;
 import hudson.maven.MavenBuild;
@@ -121,6 +122,21 @@
         return true;
     }
 
+    @Override
+    public boolean end(MavenBuild build, Launcher launcher, BuildListener listener) throws InterruptedException, IOException {
+        //Discard unneeded test result objects so they can't waste memory
+        for(MavenReporter reporter: build.getProjectActionReporters()) {
+            if(reporter instanceof SurefireArchiver) {
+                SurefireArchiver surefireReporter = (SurefireArchiver) reporter;
+                if(surefireReporter.result != null) {
+                   surefireReporter.result = null;
+                   build.save();
+                }
+            }
+        }
+        
+        return true;
+    }
 
     public Action getProjectAction(MavenModule module) {
         return new TestResultProjectAction(module);
