Client:	jenkins-${PROJECT_NAME}-${ENV_NAME}-${NODE_NAME}-${JOB_NAME}-configscripts

Owner:	somedomain\someuser

Root: ${WORKSPACE}

Options:	noallwrite clobber nocompress unlocked nomodtime normdir

Description:
	${NODE_NAME}
	
SubmitOptions:	submitunchanged

LineEnd:	local

View:
	//CM/${PROJECT_NAME}/${ENV_NAME}/Utils/...	//${P4_CLIENT}/Utils/...
	//CM/${PROJECT_NAME}/${ENV_NAME}/Scripts/...	//${P4_CLIENT}/Scripts/...
