pipeline { 
  agent any
  parameters {
    string (name: 'PARAM', defaultValue: 'val', description: 'value')
  }
  stages {
    stage ('Build') {
      steps {
	sh "echo ${params.PARAM}"
      }
    }
  }
}
